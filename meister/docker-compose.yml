
version: "2"
services:
  mongodb:
    image: mongo:latest
    container_name: "mongodb"
    ports:
        - 27017:27017
  app:
    build:
      context: .
    container_name: "batutourism-meister"
    environment:
      MONGO: "mongodb://mongodb:27017/hotels"
    ports:
      - 3002:3002
