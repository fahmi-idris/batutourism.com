
version: "2"
services:
  mongodb:
    image: mongo:latest
    container_name: "mongodb"
    ports:
        - 27017:27017
  app:
    build:
      context: .
    container_name: "batutourism-verbraucher"
    environment:
      MONGO: "mongodb://mongodb:27017/users"
    ports:
      - 3001:3001
